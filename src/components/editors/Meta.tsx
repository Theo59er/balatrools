export default function MetaEditor({ data, setData }: { data: any, setData: (data: any) => void }) {
    return (
        <>
            <p>Meta-Datei Editor ist noch in Entwicklung.</p>
        </>
    );
}